<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimaryDark">@color/start_color</item>
        <item name="android:statusBarColor">@color/start_color</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="MyMenuTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">#00000C37</item>
        <item name="android:itemBackground">@color/start_color</item>
        <item name="android:textColor">@color/text_color_light</item>

    </style>

    <style name="MenuItemStyle">
        <item name="android:itemBackground">@color/start_color</item>
        <item name="android:statusBarColor">@color/start_color</item>
    </style>

    <style name="Theme.WeatherApp" parent="Base.Theme.WeatherApp" />
</resources>